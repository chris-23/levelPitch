package io.github.cnissler.levelpitch.leveling

import org.junit.Assert.assertEquals
import org.junit.Test

/** Proves the :leveling test wiring; replaced by real engine tests in M1. */
class ModuleSmokeTest {
    @Test
    fun junitRuns() {
        assertEquals(4, 2 + 2)
    }
}
